
public enum ItemType
{
    ScrewDriver,
    Cogwheel,
    Screw,
    Key,
    Note,
    LightBulb
}

public enum ScrewType { None, Minus, Plus, Torx, Y }
public enum ScrewdriverType { None, Minus, Plus, Torx, Y, Flat }
public enum KeyType { None, Box, Door }
public enum CogwheelType { None, Small, Medium, Large }


